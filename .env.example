# Copy this file to backend/.env and point the API at PostgreSQL.
# Keep database credentials on the server; never add them to frontend runtime-config.js.
DATABASE_URL=postgresql://app_user:strong-password@db.example.internal:5432/supalai

# Optional runtime settings.
DEBUG=true
SIMULATOR_ENABLED=true
GOOGLE_CLIENT_ID=
